---
name: forge-git
description: "Safe git workflow: status, focused commits, no force-push"
---
# forge-git

- `git status` then focused `git add` of intended files.
- No `git push --force`, no `filter-branch`, no hooks that phone home.
- Do not set assumeYes.
