forge v94.0.0 — CLI-only build "gapwise" + "masterwise/tokenwise" follow-ons (gap fix / integration patch + Engineering Intelligence Core) on top of "sensewise" + "wirewise" + "corewise" + "gitwise" + "fast" + "noguard + worker clamp")
- zero-dependency Node CLI (>=20, ESM), runs directly against providers
  (no localhost server, no build, no native modules; ARM64/Termux friendly)
- 22 providers (21 OpenAI-protocol + Anthropic), auto-failover,
  measured model routing (modelstrategy), custom providers via
  `forge provider add <name> <baseUrl>`; TokenRouter included
  (TOKENROUTER_API_KEY, one OpenAI-compatible /v1 key over 300+ models)
- v94 FOLLOW-ONS (masterwise + tokenwise — no rewrite, no version bump,
  composed over the existing engines, never duplicating them):
  * Search intelligence: searchproviders.js — Firecrawl first-class
    provider (search/scrape/crawl via FIRECRAWL_API_KEY), adaptive
    provider ordering from persisted success/latency stats, DuckDuckGo
    fallback, normalized-query TTL cache, dedupe + BM25 ranking, honest
    failure (diagnosis + tried list — search failure is NEVER a fake
    "no results"); codesearch.js gained a bounded incremental chunk cache
  * Execution controller: execcontroller.js — adaptive per-segment step
    sizing, stuck detection (tool loops / repeated failures / no-progress
    segments) that forces a strategy-escaping replan, enforced resource
    fuses (failure_rate/recovery_loop → replan; wall_clock → resumable
    checkpoint); a step budget can NEVER produce COMPLETED — only
    verified completion can; fuse-parked resume crash fixed
  * Engineering memory: engmemory.js — layered memory over the existing
    stores (L1 hot / L2 working / L3 project / L4 episodic / L5 lessons +
    durable EVIDENCE store with full provenance: conversation/session/
    task/checkpoint/run); status lifecycle observation→inference→
    hypothesis→fact→verified→stale→rejected→historical, model claims
    without evidence can never become FACT; BM25+bonus retrieval,
    file-mutation staleness, consolidation (merge + contradiction
    detection), conversationId continuity, long-prompt requirement
    ingestion (R1..Rn records)
  * Predictive planner: plannerisk.js — plan risk with success/failure
    probability (damped noisy-OR over node predictions), per-node
    prediction (the previously-dead estimated_cost now consumed),
    failure forecasting (probability×impact×recovery), alternative plans
    for high/critical risk, information-gain experiments, reality delta
    (MATCH/MINOR/SIGNIFICANT/CONTRADICTION), live risk updates with
    audit history, critical path + SPOF, risk-based verification plans
    (LOW targeted → CRITICAL full + adversarial + runtime)
  * Skills pack: the Understand-Anything skills (9 skills: understand,
    understand-chat, understand-dashboard, understand-diff,
    understand-domain, understand-explain, understand-figma,
    understand-knowledge, understand-onboard) bundled byte-identical and
    wired into first-party skill routing, load_skill (64 KB + skill-dir
    header, agents/ + scripts resolvable); their knowledge graph
    (.ua/knowledge-graph.json) feeds project memory retrieval
- v94 FOLLOW-ONS (toolwise + skillwise — no rewrite, no version bump):
  * toolwise: three read-only, deterministic agent tools composed over
    existing engines (26 → 29): kg_query (project knowledge graph:
    dependents / blast radius / tests / .ua graph), plan_whatif
    (simulate plan add/remove/update through the predictive risk engine
    BEFORE committing), code_context (semantic hits + structural wiring
    in one call); capabilities registry stays 1:1 with the wire
  * skillwise: the obra/superpowers engineering-process pack (MIT,
    github.com/obra/superpowers) bundled as first-party skills — 13 of 14
    upstream skills byte-identical (upstream writing-plans NOT re-bundled:
    forge ships its own adapted one — no-overwrite policy); the 5
    platform-seam skills carry appended "Forge execution notes" over a
    byte-identical upstream prefix (subagent dispatch → delegate tool,
    script paths → [skill dir:] header, harness mapping); sha256-pinned
    against drift; MIT license shipped at skills/ root; 89 → 102 bundled
    skills, 34 first-party catalog skills
  * knowwise: the project becomes a living knowledge source —
    knowgraph.js auto-writes a deterministic FLOOR
    .ua/knowledge-graph.json (world-model extractors over the persistent
    index; no LLM, no network, no understand-anything npm deps) on the
    first task in any project; real understand-anything graphs are
    detected (generator marker) and NEVER touched; mtime+size-sum
    fingerprint rebuilds only on source drift; corrupt floor graphs are
    rebuilt; atomic write; feeds the engmemory retrieval bridge and
    kg_query through the shared parser with zero further wiring
    (KG_BOOTSTRAPPED event). impact.predictBlastRadius: every successful
    write_file/edit_file/multi_edit/apply_patch carries a bounded
    blast-radius prediction (radius · scope · importers · tests) as one
    advisory line + additive record field + TOOL_BLAST event — never
    contains "created"/"deleted", off with tools.intelligence:false or
    FORGE_BLAST_RADIUS=0. sysshell.js: one shell resolver for every spawn
    site (tools bash, sandbox, process_spawn, chat ! lines) —
    FORGE_SHELL > /bin/sh > $PREFIX/bin/sh (Termux) > $SHELL > PATH;
    forge doctor reports the resolved shell; FORGE_SKILL_ALLOW_PRIVATE=1
    opts in to private skill mirrors
- v94 DEEPWISE (judgment before action — deterministic, no model calls,
  no network, no new subsystems): plan competition + adoption — the
  original plan competes against its reshaped variants on expected
  verified progress and the winning shape is actually ADOPTED by the
  planner when it wins by a real margin (>= 0.03) at equal-or-better
  risk and success (adoptDecision; dependency-dropping shapes advised,
  never auto-adopted; predictions re-stamped, live risk restarted);
  pre-mutation self-critique — one deterministic checklist before every
  file mutation (secret paths, edit targets that do not exist,
  same-file edit thrash, hub files read from the knowwise floor graph)
  as one advisory line + TOOL_CRITIQUE event, never blocks, off with
  tools.intelligence:false or FORGE_CRITIQUE=0; reality-to-risk closure —
  repair outcomes move LIVE risk (liveRisk.experiment)
- v94 GAPWISE (no rewrite — the confirmed v93 gaps fixed in-place):
  * ONE completion contract: canCompleteFastPath shares the whole-task
    gate's module/shape/invariants; budget exhaustion without a final
    answer → INCOMPLETE/RESOURCE_LIMIT + boundary checkpoint + resume;
    run journals gain "incomplete" as a terminal state
  * workers: EXHAUSTED status (settled, ok:false, reassignable); meta's
    runner returns the full agent outcome; §35 retries exhausted workers
    once, then the node FAILS honestly
  * Runtime Intelligence: runtimesession.js (discovery with evidence,
    health probes, claim gate, ownership ledger + pid-reuse-guarded
    reconcile) + runtime tool (#26), verifier-gated
  * World model: persistent world.json + fingerprint-diff incremental
    builds + honest truncation + durable invalidate() wired to meta
    (WORLD_INVALIDATED)
  * Core bus persists (bindTask + history replay) + bounded events.jsonl
    engineering ledger + reconstruct(taskId)
  * Language: documentSymbol structured extraction, LSP-first with
    lexical fallback + provenance; layer-3 availability bug fixed
  * Tool creation: toolcreate.js lifecycle with BEHAVIORAL verification
    (child process, schema-match evidence); only ACTIVE+verified tools
    reach the agent
  * Learned skills: promotion requires recorded behavioral evidence +
    fresh fingerprint; markStaleSkills wired into meta
  * Strategy 3.0: contextual factors + justification; outcomes stored
    with context; meta records real outcomes
- v93 SENSEWISE (nothing rewritten — three new senses, 25 tools):
  * process: background process manager (spawn/poll/status/kill/list) —
    dev servers survive the tool call; poll returns only NEW output; ports
    DETECTED from output + OS socket table, never guessed; kill signals
    the whole process group; lifetime fuse = timeout-killed, never
    "exited 0"; live cap 8; all children reaped + killed at forge exit
  * repl: persistent real Node REPL sessions — variables/imports/data
    survive between calls; incomplete input reset with .break (reported);
    timeout reports "still running", never fabricates; dead sessions
    transparently restart (reported)
  * semantic_search: find code by meaning — BM25 (retrieval.js) reranked
    with provider embeddings when resolved; any embedding failure degrades
    to plain BM25; truncated scans reported; verifier-whitelisted read-only
  * island wiring completed: skillforge (agent/chat/compose/context),
    embeddings (agent/meta), memgraph (memory/lessons/compose), retrieval
    (+codesearch)
- v92 WIREWISE (nothing rewritten — the v91 engines remain the source of
  truth; every island module is now consulted by the living system):
  * P0 FIX: agent.js plugin-load TDZ — `unrestricted` was referenced before
    its const declaration; the swallowed ReferenceError meant user tool
    plugins (~/.forge/tools) NEVER loaded in any runAgent invocation.
    Declared before first use; regression test locks source order.
  * prediction ledger (prediction.js, §9): predict before every segment
    (DAG node targets + planning risk — never model self-confidence),
    settle against observed reality (filesHit/Extra/Missed, riskDelta,
    outcomeCorrect, driftScore), persist bounded per project, and feed
    real prediction errors back into the planning prompt as calibration
    (PREDICTION_MADE / PREDICTION_SETTLED / PLAN_PREDICTION_CALIBRATION)
  * language adapters wired (langadapter.js, §7/§8): every agent system
    prompt carries honest deep-vs-conservative coverage for the task's
    languages; DAG node targets get per-file adapter briefs; the 8-layer
    parse ladder stays honest (absent = UNAVAILABLE, never invented)
  * world-model consultation at planning (§5/§10): project shape + blast
    radius + covering tests of objective-named files (PLAN_WORLD_CONSULTED)
  * integrator conflicts reported (§31): integrateResults overlaps were
    computed and discarded, and core's INTEGRATION_CONFLICT handler was
    dead code — meta now emits the event in the exact claims shape core
    resolves with the evidence ladder, plus a bus WARNING mirror
  * /tools help shows the real tool count (dynamic, 22)
- v88 NOGUARD (owner's standing decision, permanent):
  * nothing is refused and nothing prompts: block-class (rm -rf /, mkfs, dd
    to devices, fork bombs, shutdown), danger/confirm, sudo, interpreter
    eval — all run, whatever the config says; the refusal code paths are gone
  * no project write boundary (writes anywhere), no sensitive-read block,
    no SSRF gate on fetch_url (private/loopback/metadata URLs fetch normally)
  * bwrap sandbox is OPT-IN (FORGE_SANDBOX=1); default is direct /bin/sh
  * kept on purpose (correctness, not guards): read-only verifier/plan agents
    cannot write (VERIFY => READ_ONLY), secret redaction of key shapes in
    tool results, socket pinning (anti-rebinding), skill/tool downloads keep
    the full netguard pinnedFetch policy
  * classifyCommand still labels every command (block/danger/confirm/low/
    safe) for logs, /status, tool-intelligence and verification risk
- v90 GITWISE:
  * git_diff / git_log / git_blame — dedicated read-only git views
    (HEAD/stage/worktree/any ref, path filter, context, line budget with
    loud truncation; log limit/stat/path; blame window capped at 200 lines).
    execFile argument arrays only, base can never be a git flag; LOW-risk
    READ class, parallel-safe, verifier-whitelisted, doctor-self-tested.
    19 -> 22 tools.
  * silent-stop fix: an empty model response (no text, no tool calls) used to
    end the run as "completed" with "(empty answer)". Now: nudge + retry on
    the same step budget (3 attempts), persistent streak fails loudly with
    exit 1. e2e: EMPTY_ONCE recovers, EMPTY_ALWAYS fails.
- v89 FAST (zero behavior change, byte-identical results — proven by
  tests/test-v89.mjs against a reference implementation of the old code):
  * xlang cross-language graph traversals are adjacency-indexed + memoized:
    was O(VxE) repeated ~40x per agent run (~60% of agent-step CPU) — an
    agent step on a 400-file repo went 1.80s -> 0.39s
  * forge.js lazy-loads the chat REPL / agent engine / tool registry per
    subcommand: CLI startup tax 218ms -> 57ms
  * provider fail-fast: connect-guard expiry (default connectMs now 8s, was
    30s) skips same-provider retries and goes straight to the failover chain
    (worst-case dead-provider stall ~94s -> 8s); transient 429/5xx retries
    unchanged
  * Anthropic prompt caching: cache_control on the static prefix (tool
    schemas + system prompt, ~16KB re-sent every agent step) — same content,
    cache-served from step 2
  * parallel test runner: FORGE_TEST_CONCURRENCY (default 4); bash suites
    stay sequential (shared port); fast lane ~85s -> ~39s
  * FORGE_DEBUG_PROMPT=<path> dumps the exact first-request payload
- v88 WORKER CLAMP: low-tier / low-RAM machines run 2 parallel read-only
  workers (floor, was 1); the absolute ceiling is 8 on any machine
  (AGENT_BUDGETS.maxParallelSubAgents), burst scaling included; mutators
  still serialize through a single writer
- agent core: bounded tool loop (agent.js) + autonomous controller (meta.js):
  PLAN -> DAG -> segments -> verification ledger -> repair -> recovery,
  crash-resumable with effect reconciliation; 9-check completion gate;
  evidence epochs (stale evidence cannot pass); worker pool with roles,
  budgets, cancellation and orphan accounting
- 29 tools: bash, read/write/edit/multi_edit/apply_patch, glob,
  grep, web_search, fetch_url, read_image (vision), browser (opt-in),
  memory, todo, think, delegate (read-only sub-agents), git_status, …
  + v94c toolwise read-only intelligence tools: kg_query (project
  knowledge graph queries — dependents/blast radius/tests/.ua graph),
  plan_whatif (deterministic plan risk simulation before committing),
  code_context (semantic hits + structural wiring in one call)
- knowledge: hierarchical memory + failure lessons + per-project claims /
  decisions / gaps; 102 bundled skills (incl. understand-anything +
  obra/superpowers packs); strict skill lifecycle
  (download -> CANDIDATE -> verify -> VERIFIED -> promote -> ACTIVE,
  TTL + drift demotion, DOWNLOAD != TRUST, learned plugins are hostless
  playbooks — never spawned, never auto-ACTIVE)
- state: sessions with resume (messages + cwd + usage), checkpoints with
  gzip backups + sha256 undo, task records with fsync durability
- self-test: `npm test` from forge/ — 158 suites incl. e2e (mock provider)
  and clean-room npm install; zero network needed; CI: Node 20 + 22
Config file: ~/.forge/config.json (chmod 600, keys stay on your device).
Requirements: Node.js >= 20 only (18 is EOL, untested).
